const readline = require("readline").createInterface;
function createInterface(input:NodeJS.ReadableStream,output?:NodeJS.WritableStream,
completr?:Completer | import("readline").AsyncCompleter, terminal boolean
)


